package test;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

public class SaveSomethingTempTest {
    // Cannot test, because HandlerInput cannot be initiated by hand. It is required for testing the Attributemanager.
    // There is jsut one simple ofCourseItRunsTest
    
    @Test
    public void itRunsTest() {
        assertTrue(true);
    }
}
